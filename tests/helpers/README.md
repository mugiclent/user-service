# tests/helpers/

Shared utilities for tests — seed functions, factory builders, and setup helpers.
Used by both unit and integration tests.

## Files

| File | Purpose |
|---|---|
| `seed.ts` | `seedUser()`, `seedOrg()` — creates minimal DB records for integration tests |
| `factories.ts` | In-memory object factories for unit tests (no DB) |
